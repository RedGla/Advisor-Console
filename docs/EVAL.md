# Evaluation Report

## Results Table

> The table below is generated by running `eval/run_eval.py` against a live development server.
> After running the script, replace the block below with the contents of `eval/results.md`.

<!-- PASTE eval/results.md CONTENTS HERE AFTER RUNNING run_eval.py -->

| id | category | prompt | response | prompt_tokens | completion_tokens | est_cost | status | latency_ms | pass/fail | notes |
|----|----------|--------|----------|---------------|-------------------|----------|--------|------------|-----------|-------|
| —  | —        | *run eval/run_eval.py to populate* | — | — | — | — | — | — | — | — |

---

## Rubric Self-Score (PRD §7.1)

Scores are 0–3 per criterion (0 = not met, 1 = partial, 2 = mostly met, 3 = fully met).

| # | Criterion | Score (0–3) | Notes |
|---|-----------|-------------|-------|
| 1 | Response accuracy — answers are factually grounded in the retrieved context | __ / 3 | |
| 2 | Hallucination avoidance — model does not fabricate sources or facts | __ / 3 | |
| 3 | Latency — p95 end-to-end response time ≤ 5 s under normal load | __ / 3 | |
| 4 | Graceful degradation — LLM provider outage surfaces a user-friendly error, no crash | __ / 3 | |
| 5 | Token & cost cap enforcement — hard limits respected per-user per-day | __ / 3 | |
| 6 | Conversation history — context window correctly includes prior turns | __ / 3 | |
| **∑** | **Total** | **__ / 18** | |

---

## Known Limitations

The following limitations are acknowledged in the PRD itself and are **out of scope** for the current sprint:

1. **Naive keyword grounding** — document retrieval is keyword/BM25-based. Semantic similarity (embeddings / vector search) is not implemented. This can cause relevant passages to be missed when the user's wording differs from the source text.

2. **No embeddings** — there is no embedding pipeline or vector store. Retrieval quality is therefore bounded by exact-term overlap, and synonyms or paraphrases may fail to surface the correct context.

> These limitations are tracked as future work. No score deduction is applied to rubric criteria 1 or 2 solely because of these two gaps, provided the system behaves correctly within its stated keyword-retrieval approach.
